# Зависимости, нужные только при разработке: тесты, метрики, генерация тестовых данных.
# Установка (поднимает сразу и основные, и эти): pip install -r requirements-dev.txt

-r requirements.txt

pytest                    # запуск тестов
jiwer                     # расчёт WER по тестовым данным
edge-tts                  # синтез тестовых аудиодиалогов
pydub                     # склейка аудиофрагментов
audioop-lts               # заплатка pydub для Python 3.13+ (audioop убрали из stdlib)
