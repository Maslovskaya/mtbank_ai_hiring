# Что НЕ попадает внутрь образа при сборке.
# Чем меньше контекст сборки — тем быстрее сборка и меньше образ.

.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

.git/
.gitignore

# Секреты внутрь образа не вшиваем — они передаются при запуске через env_file
.env

# Служебное
*.log
.ipynb_checkpoints/
